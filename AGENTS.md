# STEM101 저장소 작업 지침

## 작업 환경

- 한글 경로와 한글 내용을 안전하게 다루기 위해 PowerShell을 사용하지 않는다.
- 기존 프로젝트의 사용자 변경 사항을 보존하고, 관계없는 파일을 되돌리지 않는다.

## 상세 시뮬레이션 UI

`projects/` 아래에서 상세 시뮬레이션을 새로 만들거나 화면 구조를 크게 바꿀 때는 별도 요청이 없어도 다음 규칙을 적용한다.

1. 작업 전에 `docs/simulation-design-system.md`를 읽는다.
2. 새 페이지는 `templates/simulation-page/`의 골격과 `shared/simulation-ui.css`의 토큰·공통 클래스를 우선 사용한다.
3. 프로젝트마다 대표 색상을 정확히 두 개 정한다. `--sim-color-primary`는 바탕과 넓은 면, `--sim-color-secondary`는 조작과 핵심 변화에 쓴다. 중립색과 의미색은 공용 토큰만 사용하며 임의의 세 번째 강조색을 만들지 않는다.
4. 두 색상은 `catalog/projects.json`의 `theme.primary`, `theme.secondary`에도 기록한다. 다른 프로젝트와 같은 색상 쌍을 재사용하지 않는다.
5. 상세 페이지의 목적은 완성 자료를 홍보하는 것이 아니라 학생과 선생님이 변수를 바꾸고, 예상하고, 실행하고, 결과를 비교하며 원리를 검증하게 하는 것이다.
6. 시뮬레이션 장면을 가장 크게 두고 `질문/관점 → 조작 → 즉시 변화 → 수치·그래프 검증 → 모형의 한계` 흐름을 유지한다.
7. 작은 글자, 장식성 카드, 광고 문구, 불필요한 배지·그라데이션·둥근 모서리를 추가하지 않는다. 설명은 관련 현상 가까이에 짧게 둔다.
8. 캔버스 확대는 포인터 위치 기준, 이동은 드래그, 전체 위치 파악이 필요한 큰 공간은 고정 미니맵을 기본으로 한다. 떠 있는 패널이 시작 애니메이션으로 화면 중앙을 가로지르지 않게 한다.
9. 라벨은 대상을 가리지 않고 서로 겹치지 않도록 충돌 회피·오프셋·리더선을 적용한다. 이름은 짧게 쓰고 관계 설명은 별도 주석으로 분리한다.
10. 데스크톱과 모바일에서 주요 조작을 실제로 확인하고 `node scripts/validate-catalog.mjs`를 통과시킨다.

## SITE 반영

- 사용자가 별도로 배포를 다시 요청하지 않아도, 시뮬레이션이나 목차를 변경한 작업은 로컬 검증과 정적 빌드를 마친 뒤 GitHub Pages 공개 SITE까지 반영한다.
- 작업 트리에 관계없는 변경이 함께 있을 때는 해당 작업 파일만 선별하여 커밋하고 기존 사용자 변경은 보존한다.
- 배포 후에는 GitHub Pages 작업의 성공 여부와 실제 공개 URL에서 새 번들이 제공되는지 확인한다.
